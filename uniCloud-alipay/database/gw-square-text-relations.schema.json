{
  "bsonType": "object",
  "required": ["text_id", "category_id", "subcollection_id"],
  "permission": {
    "read": true,
    "create": false,
    "update": false,
    "delete": false
  },
  "properties": {
    "_id": {
      "description": "ID"
    },
    "text_id": {
      "bsonType": "string",
      "description": "古文ID",
      "trim": "both"
    },
    "category_id": {
      "bsonType": "string",
      "description": "分类ID",
      "trim": "both"
    },
    "subcollection_id": {
      "bsonType": "string",
      "description": "子合集ID",
      "trim": "both"
    },
    "sort": {
      "bsonType": "int",
      "description": "排序",
      "defaultValue": 0
    },
    "enabled": {
      "bsonType": "bool",
      "description": "是否启用",
      "defaultValue": true
    },
    "created_at": {
      "bsonType": "timestamp",
      "description": "创建时间",
      "defaultValue": {
        "$env": "now"
      }
    }
  }
}
