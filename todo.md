@pages/ancient/dictation.vue 拍照检查，点击后允许用户上传照片，需要将照片压缩（最长边 1080 像素，JPG 质量 65，大小控制在 150KB 左右），然后识别。读取默写纸上的古文id，然后到系统中找到原文进行比对批改，并且打分，标注默写的情况，绿色为默写对，红色为默写错的字，将批改的结果和图片一并存入到数据库。大模型识别的时候，一定要告诉大模型，不要智能校准错别字
大模型使用Qwen3.5-Plus的模型，apikey已经保存在